import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#4abcb0',
            contrastText: '#000000',
        },
        secondary: {
            main: '#cc5c24',
        },
    },
});

export default theme;